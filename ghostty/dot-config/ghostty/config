# Ghostty configuration.
#
# Intentionally empty: all keybindings use Ghostty's defaults (Cmd-d split,
# Cmd-t new tab, Cmd-w close, Cmd-1..9 tab switching, etc.).
#
# This file previously forwarded macOS shortcuts into tmux by sending literal
# `\x02` (Ctrl-b) prefix sequences. See git history if that setup is ever needed
# again.
